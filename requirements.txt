# =============================================================================
# requirements.txt — JurisBE Backend
# Python dependencies for the FastAPI RAG API and data processing
# =============================================================================

# ─── API Framework ───────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
python-multipart>=0.0.26             # Form data support for FastAPI (CVE-2026-40347)

# ─── LLM & Embeddings (RAG) ─────────────────────────────────────────────────
anthropic>=0.87.0                    # Claude API (CVE-2026-34450/34452 fixed in 0.87.0)
sentence-transformers>=2.7.0          # Embedding model (PyTorch CPU backend)
qdrant-client>=1.9.0                  # Vector DB Qdrant cloud (3.5M chunks juridiques)

# ─── Authentication ──────────────────────────────────────────────────────────
pyjwt>=2.8.0                         # JWT token handling
bcrypt>=4.1.0                        # Password hashing (GPU-resistant)

# ─── Database ────────────────────────────────────────────────────────────
psycopg2-binary>=2.9.0               # PostgreSQL driver (production)

# ─── Security ────────────────────────────────────────────────────────────
slowapi>=0.1.9                       # Rate limiting per IP

# ─── Billing ────────────────────────────────────────────────────────────
stripe>=8.0.0                        # Stripe payments & subscriptions

# ─── HTTP & Scraping (data pipeline) ────────────────────────────────────────
requests>=2.33.0                     # CVE-2026-25645
beautifulsoup4>=4.12.0
tenacity>=8.3.0                       # Retry logic for API calls

# ─── PDF Generation ────────────────────────────────────────────────────────
# weasyprint>=62.0                   # Disabled — requires pango/gdk system libs (too heavy for Railway free tier)
pypdf2>=3.0.0                        # Extraction texte depuis PDF (notes étudiants)
python-docx>=1.1.0                   # Extraction texte depuis DOCX (notes étudiants)

# ─── Text-to-Speech (Podcast IA) ────────────────────────────────────────────
# edge-tts installé dans le Dockerfile (séparé pour ne pas bloquer le build)

# ─── NotebookLM Integration ─────────────────────────────────────────────────
notebooklm-py>=0.1.0                 # NotebookLM API (storage auth — pas de Playwright en prod)

# ─── Data Processing ────────────────────────────────────────────────────────
# (used by processors/ and scrapers/)
lxml>=6.1.0                          # CVE-2026-41066
python-dotenv>=1.0.1
jinja2>=3.1.0                        # Templates HTML pour SEO programmatique

# ─── Observabilité ──────────────────────────────────────────────────────────
sentry-sdk[fastapi]>=2.0.0           # Error tracking + performance monitoring
langfuse>=2.0                         # IA observability — traces RAG, cost/qualité par question (audit #12 2026-05-09). Activation conditionnelle via LANGFUSE_PUBLIC_KEY env var.

# ─── Testing ─────────────────────────────────────────────────────────────────
pytest>=8.2.0
pytest-asyncio>=0.23.0
httpx>=0.27.0                        # Async HTTP client (requis par TestClient async)
