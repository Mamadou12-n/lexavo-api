[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
# Filtres warnings courants (chromadb, pydantic v2, etc.)
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
